from setuptools import setup, find_packages

setup(
    name="wechatlog",
    version="0.1",
    packages=['wechatlog'],
    install_requires=[
        'moviepy',
        # Add any other dependencies your project needs
    ],
)